name: Publish Vue
on:
  push:
    branches:
      - main
    paths:
      - libs/vue/package.json

jobs:
  deploy-vue:
    uses: ./.github/workflows/deploy.yml
    secrets: inherit
    with:
      package: libs/vue
