name: Deploy Angular
on:
  push:
    branches:
      - main
    paths:
      - libs/angular/package.json

jobs:
  deploy-angular:
    uses: ./.github/workflows/deploy.yml
    secrets: inherit
    with:
      package: libs/angular
